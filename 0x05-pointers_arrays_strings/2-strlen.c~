#include "main.h"
#include <string.h>
/**
 *_strlen - this function returns the length of a string
 *@s: the string
 *Return: 0
 */

int _strlen(char *s)
{
char *s;
int ln;

ln = _strlen(*s);

return (0);
}
